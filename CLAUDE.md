# CLAUDE.md

이 프로젝트의 규칙은 **[AGENTS.md](./AGENTS.md)** 에 있습니다. 먼저 읽어 주세요.

여기에는 Claude Code 에서만 다르게 적용할 내용만 적습니다.
규칙이 겹칠 때는 AGENTS.md 를 따릅니다.

---

## 이 프로젝트 한 줄 요약

이종빈(신입 백엔드 개발자)의 포트폴리오 사이트.
React 19 + TypeScript + Vite, `src/App.tsx` 한 파일에 전부 들어 있습니다.

## 반드시 지킬 것

1. **사실을 지어내지 마세요.** 면접에서 검증되는 내용입니다.
   근거가 없으면 비워두고 사용자에게 물어보세요.
2. 라이브러리를 추가하지 마세요. 순수 CSS + React 만 씁니다.
3. 작업 후 `npm run typecheck && npm run build` 를 실행해 확인하세요.

## 자주 쓰는 명령어

```bash
npm run dev         # 개발 서버
npm run typecheck   # 타입 검사만
npm run build       # 타입 검사 + 빌드
```

## 대화 방식

- 한국어로 답합니다.
- 비전공 초중급 학습자입니다. 개념 → 이유 → 코드 순서로 설명해 주세요.
- 파일이 크므로, 고칠 부분만 정확히 찾아 최소 범위로 수정하세요.