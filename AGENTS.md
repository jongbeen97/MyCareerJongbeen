# AGENTS.md

이 저장소에서 작업하는 AI 에이전트를 위한 규칙입니다.
Antigravity · Cursor · Claude Code 가 공통으로 읽습니다.

---

## 이 프로젝트가 무엇인가

**이종빈(신입 백엔드 개발자)의 개인 포트폴리오 웹사이트.**
채용 담당자와 면접관이 보는 페이지이며, 이 저장소 자체도 포트폴리오의 일부입니다.

- React 19 + TypeScript + Vite
- 외부 UI 라이브러리 없음, 라우터 라이브러리 없음
- 배포: Vercel (정적 사이트)

---

## 가장 중요한 규칙 — 사실을 지어내지 마세요

이 사이트의 모든 문장은 **면접에서 검증됩니다.** 확인되지 않은 내용이 한 줄이라도
들어가면 나머지 전부가 의심받습니다.

- 경력, 기간, 담당 범위, 기술 사용 여부를 **추측해서 채우지 마세요.**
- 근거가 없으면 비워두고, 무엇이 필요한지 사용자에게 물어보세요.
- 수치(엔드포인트 개수, 코드 줄 수 등)를 임의로 올리지 마세요.
- "팀이 한 것"과 "본인이 한 것"을 반드시 구분해 표기하세요.
- 날짜·기간 표현은 오늘 날짜 기준으로 맞는지 확인하세요.
  (예: "1년 전"처럼 시간이 지나면 틀려지는 표현은 피할 것)

---

## 파일 구조

```
jongbeen-portfolio/
├─ index.html              제목 · meta · OG 태그
├─ public/
│  ├─ favicon.svg
│  └─ decks/               발표자료 이미지 (zipmap 25장, pleegie 28장)
└─ src/
   ├─ main.tsx             진입점
   └─ App.tsx              ★ 사이트 전체 (약 3,800줄)
```

`src/App.tsx` 한 파일에 전부 들어 있습니다. **이 구조를 유지하세요.**
파일을 여러 개로 쪼개자는 제안은 사용자가 먼저 요청할 때만 하세요.

### App.tsx 내부 구역

| 구역 | 내용 |
| --- | --- |
| 1. TYPES | 타입 정의 |
| 2. STYLES | 전체 CSS 문자열 |
| 3. highlight() | 코드 블록 문법 강조 |
| 4. 공용 컴포넌트 | Nav · PageHead · DeckGallery 등 |
| 5. 페이지 | HomePage · AboutPage · WorkPage · StackPage · HistoryPage · ContactPage |
| 6. PROJECTS | 프로젝트 상세 데이터 |
| 7. ProjectPage | 프로젝트 상세 화면 틀 |
| 8. App | 페이지 전환(라우팅) |

맨 아래에 `CHARACTER`(캐릭터 일러스트)와 `PHOTO`(증명사진)가 base64로 들어 있습니다.
**이 두 상수를 건드리거나 옮기지 마세요.**

---

## 코딩 규칙

### 해야 할 것

- **타입을 먼저 정의**하고 데이터를 채웁니다. `PROJECTS`는 `Record<ProjectKey, Project>`라
  필드를 빠뜨리면 빌드가 실패합니다. 이 안전장치를 유지하세요.
- 색상은 반드시 **CSS 변수**로 씁니다. (`var(--accent)`, `var(--text)` …)
  프로젝트별 색은 `HUE` 상수와 `hueVars()`로 주입합니다.
- 모든 CSS 선택자는 **`.jb ` 로 시작**합니다. 이 범위를 벗어나면 안 됩니다.
- 새 섹션을 추가할 때는 기존 클래스(`sec-head`, `subhead`, `card` 등)를 재사용하세요.
- 애니메이션을 넣으면 `@media (prefers-reduced-motion: reduce)` 처리도 함께 합니다.
- 한국어 문장은 `word-break: keep-all`을 전제로 씁니다.

### 하지 말아야 할 것

- **Tailwind, styled-components, MUI 등 UI 라이브러리 추가 금지.** 순수 CSS만 씁니다.
- **react-router 등 라우터 라이브러리 추가 금지.** `useState` + 주소 해시로 충분합니다.
- `localStorage` · `sessionStorage` 사용 금지.
- 색상 값을 CSS 안에 직접 박아넣지 마세요. (SVG 다이어그램은 예외 — `h.a` 등 props로 받습니다)
- `index.html`의 meta 태그를 임의로 지우지 마세요.

---

## 작업을 끝내기 전에 반드시 확인

```bash
npm run typecheck   # 타입 오류 0건이어야 함
npm run build       # 빌드 성공해야 함
```

화면을 바꿨다면 아래도 확인하세요.

- **가로 스크롤이 생기지 않는가** — 1440 / 1024 / 768 / 390px에서 전부 0이어야 합니다.
- 헤더가 스크롤 중에 크기나 위치가 바뀌지 않는가 (높이 68px 고정).
- 모바일(860px 이하)에서 햄버거 메뉴가 뜨는가.
- 발표자료 이미지가 로드되는가 (`public/decks/` 확인).

---

## 자주 고치는 곳

| 바꿀 것 | 검색어 |
| --- | --- |
| 전체 색상 | `--accent:` |
| 프로젝트별 색상 | `const HUE` |
| 자기소개 문단 | `const ABOUT` |
| 프로필 표 | `const PROFILE` |
| 강점 3가지 | `const WHY` |
| 일하는 방식 | `const WAYS` |
| 입사 후 포부 | `const AMBITION` |
| 할 수 있는 일 | `const CAN_DO` |
| 기술 스택 | `const STACK` |
| 기술×프로젝트 표 | `const MATRIX` |
| 이력 | `const TIMELINE` |
| 면접 질문 | `const ASK` |
| 핵심 지표 | `const METRICS` |
| 채용 정보 | `const HIRE` |
| 프로젝트 상세 | `const PROJECTS` |
| 발표자료 경로 | `const DECK_BASE` |

---

## 글쓰기 규칙

문구를 쓰거나 고칠 때는 [CONTENT.md](./CONTENT.md)를 먼저 읽으세요.
요약하면 이렇습니다.

- 기능 나열이 아니라 **"무엇이 막혔고 왜 그랬는지"**를 씁니다.
- 한 문장에 한 가지만 담고, 길면 끊습니다.
- 과장하지 않습니다. 한계와 부족한 점을 함께 적습니다.
- 존댓말 평서형(`~습니다`)을 씁니다.
- 영어 약어는 처음 나올 때 풀어 씁니다.

---

## 프로젝트를 추가하려면

1. `ProjectKey` 타입에 키를 추가합니다.
2. `PROJECTS` 객체에 같은 키로 데이터를 채웁니다.
3. `HUE`에 색을 추가하거나 기존 색을 재사용합니다.
4. SVG 시스템 구성도 컴포넌트를 만들고 `Diagram`에 연결합니다.
5. `npm run build`로 빠진 필드가 없는지 확인합니다.

메인 카드 목록과 이력 페이지는 `PROJECTS`를 순회하므로 **자동으로 반영됩니다.**

---

## 사용자에 대해

- 비전공 신입 개발자입니다. **개념부터 단계적으로** 설명해 주세요.
- 답변은 한국어로 합니다.
- 코드를 바꿨으면 **무엇을 왜 바꿨는지** 함께 설명해 주세요.
- 디버깅 경험이 많지 않으니, 오류가 나면 원인과 확인 방법을 같이 알려주세요.